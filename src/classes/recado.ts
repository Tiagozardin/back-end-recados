class Recado {
    public id: number;
    public title: string;
    public detail: string;

    constructor(id: number,title:string,detail:string){
      this.id = id;
      this.title = title;
      this.detail = detail;
    }
}

export default Recado;